# Docker

컨테이너 단위로 애플리케이션을 묶어준다. FE, BE, DB 최소 3개의 컨테이너 필요\
컨테이너들을 묶어서 한번에 docker-compose.yml 파일을 작성한다



맥은 wsl 설치 필요 없음, 별도 설치 없이도 터미널로 SSH 접속하여 리눅스 사용 가능

AWS 배포시 데스크탑에 별도의 Docker 를 설치할 필요는 없다. 제공해주는 서버에서 자체 내장

